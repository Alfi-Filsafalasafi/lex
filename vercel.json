{
    "version": 2,
    {
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python"
    }
  ],
  "buildCommand": "pip install --upgrade pip setuptools && pip install -r requirements.txt"
}
,
    "routes": [
      {
        "src": "/(.*)",
        "dest": "app.py"
      }
    ]
  }
  
